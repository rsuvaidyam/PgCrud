export const StateAdd = (item) => {
    return {
        type: 'STATE_GET',
        payload: item
    }
}
export const DistAdd = (item) => {
    return {
        type: 'DIST_GET',
        payload: item
    }
}
export const BlockAdd = (item) => {
    return {
        type: 'BLOCK_GET',
        payload: item
    }
}